ErrorDetail:
  type: object
  required:
    - code
    - message
  properties:
    code:
      type: string
      description: "Mã lỗi nghiệp vụ (Vd: POS_001, AUTH_002)"
    message:
      type: string
      description: "Thông báo lỗi hiển thị cho user"
    details:
      type: object
      description: "Các thông tin bổ sung để debug"

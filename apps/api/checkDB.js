const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const purchases = await prisma.purchaseOrder.findMany(); console.log('Total purchases in DB:', purchases.length); if (purchases.length > 0) { console.log('Last purchase:', purchases[purchases.length - 1]); } } main().catch(console.error).finally(() => prisma.$disconnect());

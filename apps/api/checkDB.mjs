import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const purchases = await prisma.purchaseOrder.findMany({ include: { items: true } }); console.log(JSON.stringify(purchases, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
